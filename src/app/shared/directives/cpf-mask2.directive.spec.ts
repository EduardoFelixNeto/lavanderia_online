import { CpfMask2Directive } from './cpf-mask2.directive';

describe('CpfMask2Directive', () => {
  it('should create an instance', () => {
    const directive = new CpfMask2Directive();
    expect(directive).toBeTruthy();
  });
});
