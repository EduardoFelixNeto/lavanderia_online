import { PhoneMask2Directive } from './phone-mask2.directive';

describe('PhoneMask2Directive', () => {
  it('should create an instance', () => {
    const directive = new PhoneMask2Directive();
    expect(directive).toBeTruthy();
  });
});
